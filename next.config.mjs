/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cf.bstatic.com',
      'images.unsplash.com',
      'echoinnovateit.com',
      "img.clerk.com"
    ],
  },
};

export default nextConfig;
